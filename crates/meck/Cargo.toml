[package]
name = "meck"
description = "Simple library for creating texture atlases"
version = "0.1.0"
edition = "2024"

[dependencies]
glam = { version = "0.33.1" }
image = { workspace = true, default-features = false }

[lints]
workspace = true
